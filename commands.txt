This is file to check the git's functionality

STEPS:
1) git init the repository in which the file is present. (Untracked)
2) git add (Untracked to Staged)
3) git commit (Staged to tracked
4) git status
5) git status
6) git log
7) git config --global user.name ""

